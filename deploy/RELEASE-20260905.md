# 2026-09-05 全平台发布记录

- 用户授权：Web、Windows、安卓、Mac 测试包全部部署。
- 应用源提交：`dccfdecdcb7879746a031053047697822c3b6096`。
- Web / 发行清单提交：`2a3c15df937156d6f239f6a89dd133f3324ab72b`，GitHub 与 Gitee master 已同步。
- Jenkins：`zhicui-deploy #180`，SUCCESS。
- 当前 runtime：`/opt/zhicui-runtime/releases/jenkins-zhicui-deploy-180`。
- 原版本：`/opt/zhicui-runtime/releases/jenkins-zhicui-deploy-179`。
- Web build：`2a3c15df9371-20260905070903`。

## 产物

- Windows beta 1.1.0：`https://luxai.cn/download/windows/Zhicui-Setup-1.1.0-x64.exe`。
- 安卓 beta 1.3.1 / build 23：`https://luxai.cn/download/zhicui.apk`。
- Mac 测试 1.1.0，arm64 / x64 DMG 与 ZIP：`https://luxai.cn/download/mac/test/dccfdecdcb7879746a031053047697822c3b6096/manifest.json`。
- Mac 构建：GitHub Actions `33950689121`，两项成功。

## 验证与限制

- 前端类型检查及首页分类测试通过；28 项相关后端测试及 3 项 subtest 通过。
- Windows Agent、发布契约、更新策略和 Mac 路径/发布配置检查通过。
- Android 完成静态生产构建、Gradle 构建、签名及版本身份校验。
- Jenkins 的加密备份、隔离恢复、readiness、权限边界、真实 AI SSE 与客户端发行检查全部通过。
- 公网首页、登录页与安装包 HEAD 返回 200；Android/Windows 公开清单版本正确。
- 所有产物经 TLS 虚拟主机的 HTTPS 源站全量哈希回读；Mac arm64 DMG 另完成公网全量 SHA-256。额外的 Windows 本机重复下载因外网速度较慢超时，不能声称该次下载完整成功。
- Windows 未签名、安卓使用现有 Debug 测试证书、Mac 未 Developer ID 签名或公证；stable 清单仍 unavailable，不绕过认证闸门。
- Mac 真实设备交互未验收，OpenSpec 不归档。Agent 外部接口维持原来的 dark 状态，未额外开放权限。

## 运维备注

- Windows 老发行脚本暴露 PowerShell 管道退出码与 CRLF 问题，已修复。安装包复用已验证缓存，不重新生成字节；上传前逐项校验安装包、blockmap、feed、manifest 哈希，再原子替换 beta 指针。
- Windows 前版 feed / manifest / 下载指针备份：`/tmp/zhicui-win-dccfdecd/rollback/`；版本化旧安装包保留。
- Nginx 原配置备份：`/etc/nginx/snippets/zhicui-windows-updates.conf.before-dccfdecd`。
- 错误换行产生的两个异常目录已移到 `/tmp/zhicui-win-path-recovery-dccfdecd` 和 `/tmp/zhicui-win-manifest-path-recovery-dccfdecd`，没有删除用户资料。
- 可选 OmniRoute 子模块拉取超时，按已有策略保留服务器已安装的 sidecar；旧 runtime 缓存清理权限警告未影响部署成功。
- 为保留用户工作区和暂存区，本次通过独立 Git index 生成发布提交；本地 master checkout 仍在旧 HEAD，`codex/production-20260905` 指向已部署版本。不要把当前全部 diff 再次当作未发布功能。
