# 账号与 Agent 接入发布状态

## 已完成代码与本地测试

- 支持邮箱：1592880030@qq.com。
- 电脑设置“登录手机”生成五分钟二维码，手机登录页扫码、电脑核对确认、手机一次性领取新 JWT；新旧扫码方向隔离。
- 并发绑定/领取、过期、取消、账号禁用和管理端权限回归通过；普通用户调用真实管理接口返回 403。
- 后端 Agent/旧扫码测试 51 项，手机/JWT/隐私/路由测试 23 项；CLI 43 项；相关前端测试及生产构建通过。
- 原生相机和已安装客户端的双端验收待新包发布后进行。

## 外部配置

- Agent 完整 Stable 仍要求真实 SMTP、模型/解析与连接器就绪，不能只打开全局开关。
- 当前邮件实现是验证链接，不是验证码免密登录。SMTP 账号和授权码必须通过服务器安全配置，不写入代码或聊天。
- `@zhicui/cli` 尚未出现在 npm 公共 registry，本机 npm 未登录，发布需要所有者授权。
- 苹果签名、Windows Authenticode 和 Android 正式签名仍遵循原发布规则；没有签名不将测试包宣称为正式签名包。

## 文档工作流已停用

按用户要求移除 OpenSpec。原有记录（含未提交内容）和相关项目内 skills/命令移至本机 `D:\6month-backups\openspec-20260906-0300`，可恢复。后续直接编码、测试和记录发布证据，不再生成 OpenSpec 文档。
