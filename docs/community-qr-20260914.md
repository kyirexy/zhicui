# 社群二维码更新（2026-09-14）

- 用户授权将其提供的新微信群二维码更新到 `https://luxai.cn/community`，仅展示截图中的二维码部分，并直接发布正式环境。
- 从生产提交 `084a9e405f64843c67916295ba4fdcd79c1b9f21` 建立独立 worktree，仅发布二维码图片、引用、有效期和相关验收记录。
- 原图 450×680，精确裁剪区域为 `(55, 225, 340, 340)`；二维码四周保留 20px 静区，移除截图中的头像、群名和底部有效期文字。
- 最终 PNG 来自原图逐像素裁剪，未重绘或缩放。ZXing 对原图与最终图均解码成功，二维码载荷一致。
- 资源路径：`frontend/public/images/community/wechat-group-20260914.png`，28,240 字节，SHA-256：`f0d079a6ebf3677915da2d68e8c5630becc8a7f579262e3438c0be06a265deea`。
- 邀请截止时间更新为北京时间 `2026-09-21 00:00:00`；页面文案、图片替代文本、保存文件名和已有边界测试同步更新。
- 17 项社群入口、匿名访问、移动反馈、文件导出及有效期回归通过，`git diff --check` 通过。
- 本次范围为正式网站，共用社群组件的官网入口同步更新。客户端安装包不在此次发布范围。

## 发布验证

使用现有 Gitee → Jenkins 流水线执行生产构建、备份、恢复校验、原子切换及发布闸门；完成后补记构建编号、公网资源校验和浏览器验收结果。
