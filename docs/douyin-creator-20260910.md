# 抖音博主同步接通

日期：2026-09-10。基于线上 `c33515b` 的隔离工作树 `codex/douyin-creator-20260910`。

## 根因与修复

- 线上抖音平台从未完成博主连接验证，因此入口禁用。旧 sidecar 虽声明 `creator_catalog`，但实际没有四个博主路由。
- 旧主服务对所有博主识别错误生成占位姓名，近期作品又回退到 `auto-collect mode=post`；该旧执行器忽略目标 URL，只读取当前账号自己的作品。已删除这两种错误回退。
- 新增独立主页、近期、分页目录和取消接口及版本握手。目标主页 ID 与每条作品作者必须匹配；所有响应均按公开元数据白名单输出，没有 Cookie、媒体 URL 或持久媒体下载。
- 主页资料接口受限时，通过归属已验证的公开作品读取真实作者名。支持整段抖音分享文案、`v.douyin.com` 短链和 `www.iesdouyin.com/share/user/` 落地页，最终只保存规范主页。
- 分页绑定用户作用域、博主、范围与游标；重复页去重，同页重试复用响应，游标循环和空异常响应不会伪装成完整成功。近期跳过平台置顶项和图文，以普通视频列表选取 20/50/100 条。
- 近期、全量都把公开元数据保存到知萃的博主目录，不混入本人喜欢/收藏/作品。转写直接使用所选博主的资料；重复转写复用现有文稿。
- 抖音全量刷新采用增量保留：未出现在本次公开目录中的历史条目不会因此变成不可见；用户主动移除状态保持原样。
- 管理连接测试必须真实验证主页及作品读取，并记录成功测试审计；只访问健康接口不再开放平台。

## 验证与发布

- 相关后端与真实 FastAPI 路由测试：54 项，53 通过，1 个既有测试跳过。覆盖错作者、官方已接受合作、假空结果、跨用户取消、游标乱序、分页去重与重试、置顶/图文过滤、旧兼容路径禁用、历史保留和转写元数据来源。
- 用户提供的公开博主为「GGBond的小课堂」。生产现有账号作用域只读探测成功：首屏 23 项（包含平台置顶），全部归属于目标博主。没有用这些旧置顶项冒充最新视频。
- 伴随服务增量发行 `/opt/douyin-downloader/releases/creator-20260910T065055Z`，通过路由预检及实际协议健康检查；原发行 `/opt/douyin-downloader/releases/c8ddfeb9-20260826135014` 留作回滚。会话和历史资料原位保留，没有安装新依赖或删除媒体。
- 主应用 Jenkins 216 已成功发布 `49aff0f48422`，公网构建标识对应，健康与真实用户旅程验证通过。
- 真实第二页验收发现官方作品列表包含与「派蒙科普」的合作视频。补充验证 `cooperation_info.co_creators` 中目标账号的已接受身份，保留主作者及“合作”标注；普通提及、未接受邀请和无关作者仍拒绝，不能简单放宽身份校验。
- 真实近期前20条和跨页82条元数据探测通过，重读相同游标返回一致，点赞/收藏/文稿/来源台账和隐藏记录未发生变化。
- 已通过鉴权 API 验证并开放抖音功能，保存「GGBond的小课堂」，完整分页任务成功保存90条视频目录，最新首条是2026-08-04的快速排序教学。
- 勾选转写实测发现既有任务条目 ID 前缀加 UUID 共49字符，超出生产 PostgreSQL 的 VARCHAR(48)，SQLite 测试未施加长度限制。缩短新 ID 前缀，保留完整 UUID 随机位和已有记录，无数据库迁移；补充真实创建后长度回归。
- 文稿与最终发行结果将在完成后补充。
