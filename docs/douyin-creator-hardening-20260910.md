# 抖音博主同步健壮性与两端正式部署

本次基于已上线的 `deb4bf721296` 独立检查，不混入主工作区其他修改。

## 修复

- 合作视频按用户、博主、平台和视频保存独立目录；同步另一个博主不会移动现有目录项。文稿继续按用户和视频复用。
- 新博主目录继承该用户对同一作品的永久隐藏或删除墓碑；只有明确恢复才重新显示，不影响其他用户。
- 分页数量上限失败时不修改已确认去重集合；同页并发只请求一次，取消在途请求不推进分页状态，异常风险字段返回受控错误。
- Windows 可变更新清单使用 Nginx 精确匹配，避免落入版本文件的长期缓存规则；版本化安装包与 blockmap 仍长期缓存。

## 数据迁移验收

PostgreSQL 在单事务和表锁内建立新的 source 唯一约束、移除旧约束，不重建表、不修改行或记录 ID。锁内重新读取约束，兼容并发启动、旧约束改名、已有等价新约束，遇到同名异列停止并回滚。部署通过 systemd restart 先退出旧进程，再执行新 startup。

已在实际 PostgreSQL 随机独立 schema 验证 5 场景：并发执行、带引号旧名称、已有新约束、等价新约束、名称冲突回滚；全部通过。每个 schema 仅含验收夹具，结束后全部清理，未访问或修改现有业务表。

生产变更前记录摘要：294 条文稿、361 条本地抖音资料、403 条来源台账、9 条隐藏记录、90 条博主目录。完成部署后核对迁移未改变原记录，并再次同步目录、重复提取现有文稿验收增量复用。

## 两端兼容

现有 Android 1.3.7（29）的前端功能源码与本次一致，调用正式 API；Windows 1.1.4 原生源码未改变，加载正式网站页面。本次无需重复打包即可在两端生效。保留现有 Beta 安装包渠道及签名约束，未将其改称 Stable 包。

两端的编译、更新和发行契约、公开页面窄屏验证及发行文件回读详见 `client-creator-compatibility-20260910.md`。65 项同步/迁移回归中 64 通过、1 项既有 POSIX-only 跳过；另有 18 项发行/下载测试通过，迁移与永久隐藏变更经独立复核。最终流水线与记录复验结果完成后追加。

## 首次发行与生产复验

- Jenkins 219 成功发布 `d5531c44a7c577900c6f5961d161d27487abbcbe`，公网构建 `d5531c44a7c5-20260910094838`；备份、隔离恢复、readiness、真实用户旅程通过。Sidecar 为 `creator-20260910T094122Z`。
- 目录唯一约束迁移后，变更前记录摘要逐行完全一致，包括全部90条目录。未改记录 ID、隐藏状态或历史文稿。
- Windows `beta.yml`、`latest.yml` 公网 200 且 no-store；尚未开放的 `stable.yml` 404 也返回 no-store。版本化安装包保持 immutable。Nginx 运维资产通过既有预安装脚本更新和配置检查。
- 重复同步首轮上游分页响应不完整，已确认44条，原有90条仍保留；任务进入自动重试，未将部分结果冒充完整成功。
- 此真实异常暴露旧迁移调度字段问题：`creator_sync_runs.next_retry_at` 是无时区 TIMESTAMP，而模型为带时区类型，生产连接时区 Asia/Shanghai。UTC 参数写入后成为本地时间，Python 又按 UTC 解释，导致重试错后8小时。SQL判断已到期但 Python 拒绝领取。
- 补充迁移仅将旧任务重试/租约字段转换为 TIMESTAMPTZ，按原会话时区还原真实时刻，不改媒体日期；新补列直接声明带时区类型。生产实际仅一条等待任务的 next_retry_at 非空，其他现存租约/调度列已带时区。
- 调度迁移已在 PostgreSQL 随机隔离 schema 验证 Asia/Shanghai 和 UTC 两种连接时区：旧记录真实时刻保持，重复执行不二次转换，新写入30秒仍为30秒，索引及媒体日期保持不变。两场景通过，临时 schema 全部清理。
- 补充调度、同步和目录相关回归33项全部通过；独立审查通过。
- Jenkins 220 成功发布 `89c77c1050b5a23b481e8aca2625f7f72389fe5d`。调度列实际为带时区类型；先前同一等待任务自动恢复，attempt_count 保持1，完整发现90条并 succeeded，next_retry_at 清空，未重新创建替代任务。
- 真实重复提取复用1条既有文稿，新增0条。再次完整同步后目录 ID 集合未变，全部原文稿/来源台账/喜欢收藏/隐藏记录摘要未变。
- 最后复核调度器的快速完成边界：已完成 Future 注册回调会同步执行，原实现持有非重入锁注册，可能使清理回调锁死扫描线程。将回调注册移到锁外，继续用 Future 身份比较保护同任务的新提交。
- 最小回归在旧实现确定复现死锁，修复后2项全部通过。最终构建前仅清理停用的 Jenkins 213 可再生成 Python/Node/Next 构建目录，保留源码、后续发行和全部业务资料，可用空间恢复约5 GiB。
