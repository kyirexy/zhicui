# 抖音同步窗口、资料目录和文案队列修复

日期：2026-09-14。基于生产提交 `d5b63c4298cd303a90328deaca14aaac88c21dc4` 的独立工作树，保留同期反馈群页面改动；未覆盖主工作目录未提交内容。

## 问题与处理

- 原采集器每个分类结束都关闭浏览器，多来源队列又自动开始下一项，造成点击显示窗口附近关闭、等待重开的体验。现在同一用户、同一同步批次复用浏览器及操作锁；最后一项、取消、失败或空闲超时释放。需要用户操作时停止后续来源，恢复当前窗口；取消整轮同步。显示窗口合并重复请求并限制等待时间。
- 最终复核补上跨入口取消归属：新增按sessionKey取消的桥接能力，原子核对活动/保留会话，并在await前捕获旧实例；旧页面收尾不得全局取消另一任务。LOCAL_ACTION_BUSY停止后续来源；异步清理后再次核对页面任务归属。
- 官方分类列表已有可信排序但缺作者或封面的作品被旧质量缓存隐藏。现在这类作品先进入资料目录，继续准备文案；仍拒绝无有效元数据的占位数据。编辑、AI 处理和旧资料恢复不改官方排序。
- 只要有本地资料，旧版目录原先就被整体跳过。现在后台一次性补存当前用户、当前绑定的旧目录，仅归档公开元数据，立即返回已有资料；前端最多静默刷新30秒。恢复失败保留已有资料，稍后重试。详情和封面优先已有目录，避免先等旧连接器。显式刷新仍可更新旧目录。
- 文案任务已有批次时，后续同步结果原先直接跳过；请求还可能被静默截断。现在分块预留所有任务、最多并发提交三批，不等待前一批转写结束；本轮同步范围内未完成任务可以补做，已完成和已运行条目去重。保留后端ASR并发控制，界面显示全部任务的完成数量，不把4个处理槽当作总数。
- 恢复卡改为短状态、必要按钮、折叠帮助；桌面账号区去掉重复技术信息。保留真实失败、未完成数量和下一步操作，不展示内部错误原文。

## 验证

- 后端74项相关回归通过：目录增量与去重、严格排序、跨用户/绑定隔离、旧目录恢复、显式刷新、详情/封面、提取任务持久化和并行、账号数据导出。
- 前端65项相关回归、7项桌面兼容/手动同步策略、5项同步反馈脚本检查通过；TypeScript检查通过。
- 桌面平台账号检查通过；真实Chrome隔离夹具验证喜欢/收藏共用一次启动与关闭，以及需要操作后恢复原窗口。
- Next实际页面浏览器夹具17项通过：取消/中断停止来源队列、共享sessionKey、50条全部提交、8/50总进度、重复完成项不再提交、处理中只追加10条新任务并汇总为16/60、历史目录恢复后停止轮询，以及280/390px卡片布局。无React或浏览器运行时异常。使用合成API和桌面桥接，不代表真实抖音账号同步已经验收。
- 1.1.7追加5项浏览器检查：失败和忙碌收尾限定本轮sessionKey，忙碌不再推进收藏，1.1.5/1.1.6旧桥接不自动全局取消，无运行时异常。新增native回归覆盖busy竞争、空闲到期后新操作及旧关闭迟到三个归属竞态。
- 本机Webpack生产构建与类型检查通过，40个页面生成成功。独立工作树的跨目录node_modules链接不适用于Turbopack，生产由Jenkins正常安装依赖并构建。
- 公网发布、安装包回读结果在完成后追加。

## 发布范围

Windows继续既有Beta通道，最终版本1.1.7。1.1.6安装包已在中间步骤发布，最终复核发现跨入口取消归属问题后追加修复并升版，不覆写同版本安装包。窗口生命周期和取消归属改动需要安装1.1.7；资料展示和网页交互改动由网页/服务端部署生效。此轮不开放Agent Stable或发布新的CLI/npm版本。

构建、截图和部署证据：`D:/6month/.codex-artifacts/douyin-sync-flow-20260914/`。

## 发行记录

- Windows1.1.7构建源码：`ba1cf497ce52f4a099bb31f3f94f322232177414`。
- 安装包：`Zhicui-Setup-1.1.7-x64.exe`，93,675,320字节。
- SHA-256：`6161d88a895c05588a42b13e7bbcb18c6df0f60d133c048ff5e8ba0897092f3c`。
- 既有Beta发行脚本完成隔离构建、内置CLI与更新契约检查，并上传版本化安装包、blockmap及beta feed。
- 公网HTTPS完整回读通过：安装包12段合并后的大小、SHA-256和SHA-512均匹配；manifest、blockmap、beta.yml一致。回读未执行安装包，证据为`public-beta-1.1.7-20260914T045743Z-7b41220e/verification.json`。
- 网页/后端部署提交：`03e528af50c57cba13c9671c2e76c523d03d4a6c`，已推送GitHub和Gitee master。Jenkins237于2026-09-14 13:11（北京时间）成功，19个发布闸门全部通过，无需回滚。
- 运行目录：`/opt/zhicui-runtime/releases/jenkins-zhicui-deploy-237`；前端、后端、抖音sidecar均active。公网health=ok、readiness=ready，Agent维持dark。
- 公开网页构建标记：`03e528af50c5-20260914050952`；实际引用JS确认包含精简恢复卡、任务追加队列、总进度和按批次取消共13个标记。报告为`public-web-verification.json`，生产证据为`production-verification.json`。
- 可选集成子模块下载超时后沿用已安装sidecar；发布后旧runtime/npm缓存自动清理给出非阻塞提示。当前根分区仍有约3.20GB可用，服务与发布验收正常。本轮已在发布前限定清理229/230的构建目录约4.41GB，未删除业务资料。
