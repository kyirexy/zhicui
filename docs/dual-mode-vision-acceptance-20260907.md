# 双模式多模态与 Codex 接入验收

## 需求与实现

- 保留平台多模态模型和用户自带模型两条路径。管理端已有供应商、模型、启用/停用、真实图片测试、方案发布版本、价格、免费额度、运行用量与账户调整能力，未重复建设。
- 本次补充管理端双模式标记，区分平台基础费、自带模型基础处理费、公共处理费及平台免费额度；修改方案仍需发布新版本，不影响旧报价快照。
- 自带模型不再占用平台赠送的免费额度；供应商费用由用户自己的账户承担，仍不允许静默回退到平台密钥。
- 修复管理端及自带模型测试共用 PNG 损坏的问题。原 IDAT CRC 不匹配且 zlib 解压失败。新探针生成有效 96×96 双色 PNG，验证实际颜色，不能用“图片可见”敷衍通过。
- 原任务层已有免费额度耗尽拦截。本次新增回归验证，没有把原有逻辑误报为新增修复。

## 已验证

- 视频解析单元测试 31 项通过，涵盖计费、额度、安全与执行引擎。
- 前端 TypeScript 检查通过（运行前生成独立工作树的 build-version 文件）。
- 候选图片探针通过生产现有服务的真实 `Qwen/Qwen3-VL-8B-Instruct` 调用；不涉及用户资料、不改默认聊天模型。
- 生产创建 `platform-qwen-vision` Provider（保持停用）及 `dual-mode-visual` 双模式方案草稿，启用自带模型选项，记录管理审计。没有擅自发布免费额度或积分价格。
- 将经过 43 项 CLI 测试的本地验收包安装到 `C:/Users/MAR/AppData/Local/Zhicui/cli-acceptance`，使用实际 Codex 0.153.4 MCP 管理命令添加 `zhicui`，配置备份成功，Skill 已安装；重复 setup 返回 changed=false，doctor 通过。
- 实际 CLI 设备登录返回 `INTERFACE_DISABLED`。这是当前生产 Agent 尚未开放的真实结果，不代表登录验收通过。

## 待验收

- 生产提交部署结果另行补记。
- 平台价格/免费额度待所有者确定；普通用户只能选择已发布方案，不能看到管理员密钥。
- 抖音/B站真实公开主页解析测试及其管理端新鲜度状态，仍不能只用 sidecar 协议健康代替。
- 完整 Agent 的 dark → 恢复快照双启动 → Stable 冒烟、Codex 真实授权登录及工具调用、npm provenance 发布均未完成。
- 本地验收包不等于已发布 npm 包；正式包发布后应将本地验收入口升级为正式包，再复验。
