# 首页最新同步预览发布

## 发布范围

- 基于当前正式版 `ca699d811c9589125b09e58a4f24b92f64e04db9` 的独立 worktree 整理；不直接发布主工作区其他未提交修改。
- 保留线上已发布的同步快照、分批偏移、顺序可靠性、旧请求拦截和登录会话保护。复用 `source_synced_ats/source_ranks`，不引入第二套来源元数据。
- 抖音旧连接器按同步批次优先排序，合并列表在截取首页预览前使用当前分类的台账；旧台账不能覆盖更新的连接器快照。
- B站首页分别查询收藏、喜欢、导入，服务端先按分类过滤再执行 500 条上限，前端按该分类快照和平台顺序取前三条。
- 同步登记/导入成功后清除首页缓存并通知刷新；窗口恢复可见或重新获得焦点时重读。缓存版本升为 v7。
- 同账号刷新失败时保留最近成功结果，成功返回空分类时正常清空；过时请求不能覆盖新结果，切换账号不复用旧账号结果。
- 不改数据库结构、部署配置、客户端安装包或平台凭据。

## 验证与发布

- 前端来源分类、分批导入、分类顺序、同步通知和登录会话 28 项定向测试通过。
- 后端本地抖音、平台导入、能力安全、采集恢复、媒体能力及路由清单 61 项定向测试通过，包含分类过滤早于 500 条上限等新增回归。
- 已生成隔离目录的构建版本文件；全量 TypeScript 检查通过。正式 Jenkins #210 发布成功。
- 正式发布通过现有 Jenkins → deploy/deploy.sh，执行加密备份、隔离恢复、独立 runtime 构建、原子切换、readiness 和真实冒烟流程。
- 桌面客户端使用线上页面，发布后刷新即可加载；Android 内置前端未在本次重打包。
- 历史 B站资料缺少可靠来源位置时，需要重新同步对应分类来补齐，不能从视频发布时间恢复真实收藏顺序。

## 本次发布记录

- 用户明确授权直接部署到正式环境。
- 提交 a8d3d86be1dc5bcc73caeeb448eadec47dd5cb6f 已推送 GitHub/Gitee master。
- Jenkins #210 于 2026-09-09 15:13:44 CST 完成，结果 SUCCESS。

- 正式 runtime：/opt/zhicui-runtime/releases/jenkins-zhicui-deploy-210，运行 Git SHA 与发布提交一致。
- 公网 build-version：a8d3d86be1dc-20260909071215；HTTPS health=ok，readiness=ready；前后端 systemd 服务均 active。
- 加密备份、隔离恢复、生产资产、发行清单、前端生产构建、登录与权限、客户端下载哈希、真实 AI SSE 及隔离冒烟数据清理全部通过。
- 可选集成子模块下载超时后按既有流程继续使用服务器已安装版本；发布结束存在先前手工 runtime 缓存权限清理警告，不影响本次发布验收。
- 本轮未对用户真实抖音/B站账号执行重新同步。桌面刷新加载新版后，历史 B站资料需重新同步对应分类以补齐可靠顺序。
