# 2026-09-09 本地项目清理

本轮只清理 `D:\6month`，没有重新发布或修改生产服务器。主工作区原有未提交修改保持原样，未执行 git clean、reset 或历史重写。

## 结果

- 138 项操作完成，无遗留失败：52 项删除，86 项移出项目归档。
- 删除文件逻辑大小 17,302,849,174 字节，约 17.3 GB（16.11 GiB）；包括旧 Next 构建、停用测试环境、旧桌面打包输出、渲染逐帧缓存、废弃下载、重复视频和默认 SVG。
- 归档文件逻辑大小 2,817,032,723 字节，约 2.8 GB（2.62 GiB）。归档路径：`D:\6month-archive\cleanup-20260909`，保留原项目相对目录结构。
- 已归档 6 个有明确替代实现的旧组件：LibraryChat、PlanCard、PlanExecutionOverview、PlanTaskList、PlanDynamicField、PlanDeleteDialog。
- 已归档无当前引用的 43 张前端根目录截图、旧验收记录、临时诊断文件、独立参考/实验项目，以及 6 份旧版宣传源工程。
- 已删除 6 张无引用的默认或旧 SVG；重复媒体按 SHA256 核对，保留唯一归集副本和当前工程。
- 补充忽略规则，防止工具产物、误生成的 `%SystemDrive%` 缓存和旧验收输出再次混入版本管理。

## 保留范围

- `参考项目/douyin-downloader` 的实际同步服务、所有本地修改和约 7.8 GB 历史下载资料。
- 数据库、环境配置、登录信息、签名材料、主依赖环境、有效 ASR 模型和当前安装包；两个有本地修改的正式子模块源码均保留。
- 当前 `videos/zhicui-vertical-demo` 工程、新版录屏脚本、Fish 配音、原始录屏和品牌原图；旧成片的唯一归集副本保留在 `videos/zhicui-promo-final`。
- 当前和上一版桌面产物 `release-1.1.0`、`release-1.0.10`；全部公开下载清单及资源。
- 所有注册 Git worktree 及 Git 历史。

## 核验与恢复

- 清理后 2,790 个其余受 Git 管理文件 SHA256 与清理前一致，148 个归档受跟踪文件内容一致；另有 1 个本轮明确更新的 `.gitignore` 已按新哈希核对，保留的 public 资源全部哈希一致。
- 删除文件逐项确认源路径不存在；归档按字节数、文件数核对，单文件同时核对 SHA256。目录链接只处理链接本身，未递归删除链接指向的依赖目录。
- 清单和执行结果位于 `.codex-artifacts/cleanup-20260909/cleanup-plan.json`、`cleanup-results.json`、`verification.json`；项目外归档附有恢复说明。
- 归档内容可按相同相对路径移回；恢复前确认原路径没有新文件，避免覆盖后续工作。直接删除的构建缓存需重新构建，重复资源可从清单记录的保留副本恢复。
- 全部删除和归档完成后，前端 `npx tsc --noEmit --incremental false` 通过；没有为清理操作引入测试框架或修改业务逻辑。
