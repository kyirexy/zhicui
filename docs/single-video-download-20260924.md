# 单条解析恢复与视频下载（2026-09-24）

## 生产问题

实际失败作品为 `7616369026764347109`，原始分享链接为 `https://v.douyin.com/JUV4bfM5wBU/`。
正式日志显示绑定下载器的作品缓存与媒体接口均返回 404，旧页面却提示“已通过绑定账号找到该作品”。
原因是缓存和作品详情均为空时仍用链接 ID 合成了元数据；媒体、无音频和 ASR 异常也被同一个提示覆盖。
一次独立公开元数据检查成功，但后续媒体预检未完成，因此不能把元数据成功当作视频或文稿已取得。

## 本次修改

- 绑定信息为空不再合成已找到的作品；公开恢复最多一次，必须匹配原作品与平台。绑定媒体仅在 404/410 时恢复，登录、验证或限流不连续重试。
- 保留原始公开短链读取上下文；已有完整资料复用时也可补充短链，不重复转写。
- 真实元数据成功后先保存当前用户的资料；正常完成更新同一条资料，保留收藏/喜欢分类和已有非空元数据。无音频保存为正常内容状态。
- 单条失败页面保留预览；已有资料 ID 时显示“下载视频”。成功后的详情页也显示下载按钮。
- 新增普通 Web JWT 接口 `GET /api/notes/{note_id}/video/download`，只下载当前用户的资料。准备完成后再次检查账号、归属与平台连接状态。
- 下载视频完整校验后返回 MP4，最大 512 MB。每用户同时 1 个、全局同时 4 个、每用户每分钟 6 次；临时文件和并发槽在结束、断连及 Range 错误时清理。
- 浏览器显示准备状态和真实接收进度，支持取消与中文失败提示；JWT 只在同源请求头中传输。

## 验证

- 后端全量 923 项，1 项按环境跳过，其余通过；覆盖归属、撤销、绑定错误恢复、无音频、重复资料、临时文件清理和既有 CLI 下载。
- 前端下载与单条路由共 25 项测试、类型检查通过。
- 生产构建与隔离浏览器验证完成后另记结果；隔离夹具不代表真实抖音平台读取成功。
- 正式发布继续走 dark、同备份恢复双启动、core 晋级；Agent 公开 Action 保持 41 项，未扩大旧令牌权限。

真实平台若要求登录或验证，必须通过正常账号连接处理；本次不规避平台限制，也不承诺任意视频都能下载。

## 9 月 25 日桌面下载补充修复

- 桌面播放器此前把普通 CDN/导入播放地址及普通封面地址传给仅接受签名能力地址的缓存 IPC，触发“媒体地址不在可信来源内/与当前视频不匹配”。
- 已有资料 ID 时统一使用 JWT 下载按钮，隐藏重复缓存下载入口，阻止播放时误触自动缓存；已有本地副本仍可播放、打开位置和删除。
- 尚未生成资料的旧抖音缓存流程仅接受本站当前作品的完整签名地址；下载前刷新能力地址，忽略不兼容的普通封面，IPC 错误映射为中文提示。
- 下载相关 11 项测试、类型检查、生产 webpack 构建通过。隔离浏览器 15 项验收通过，额外模拟旧桌面桥接与自动缓存开启，确认详情仅一个下载按钮、缓存 IPC 调用为零、JWT 下载完整 MP4 文件。该结果为本地夹具，不替代真实平台验证。
- 本次为 Web/服务端发布，不修改桌面二进制或放宽 Electron 来源校验。正式发布结果后续单独记录。

## 正式发布结果（2026-09-25）

- 目标提交：`8ea57e209e05973dcb987db218561ad10c5ec85f`，包含上一轮单条解析修复 `4b0dec9`。
- Jenkins dark：`jenkins-zhicui-deploy-307`，SUCCESS。随后使用该次加密备份完成隔离库恢复和双启动演练，隔离库已清理。
- core：`manual-single-video-8ea57e2-20260925T100230`，18:08:38（北京时间）发布成功；备份、readiness、认证、PAT/MCP 生命周期与真实 AI SSE 问答及引用全部通过。
- 正式证据：`/var/lib/zhicui-deployments/manual-single-video-8ea57e2-20260925T100230.json`，SHA-256 `0880838eca75f7b114693b7b07f6a69c012cdd79d3330fbbdfbb45c10462e867`。
- 公网 `/build-version.json` 返回 `8ea57e209e05-20260925100649`；健康接口 HTTP 200；未登录下载 HTTP 401；公开 Agent capabilities HTTP 200、core 41 项 Action。
- 额外隔离 Electron 43.2.0 验证通过：开启 sandbox/contextIsolation/webSecurity 和同源导航限制后，本地生成的 1,685 字节 MP4 经 Blob 下载成功保存，字节一致。

### 真实视频全流程

使用专用空白非管理员冒烟账号，针对上文失败链接 `JUV4bfM5wBU` 进行一次真实公网全流程：

- 作品信息读取 1.19 秒；文稿提取于 82.48 秒完成，129 字；AI 整理、保存于 107.42 秒完成。
- 新鉴权下载接口 HTTP 200，得到 **7,223,138 字节**的 MP4，视频 720×1280、包含音轨、时长 29.906009 秒。
- 文件 SHA-256：`dcdefc7552905cc038ab4b8254b0479e7c0b375d463a62e2f85a2c9b8d5086d7`。总耗时 129.90 秒，ffprobe 验证通过。
- 测试结束删除专用账号生成的 1 条资料，临时 MP4 随上下文清理；密码租约已恢复，明文凭据已删除，无并发密码修改。
- 第一次额外测试脚本未加载生产环境文件，误连空 SQLite，在发送解析请求前失败。修正脚本环境后上述一次实际视频请求通过；应用发布闸门本身未失败。
- 构建时 GitHub 可选子模块拉取超时，按现有发布策略保留已安装 sidecar；未修改集成服务或桌面二进制。npm 大包下载缓慢，但生产构建最终通过。
