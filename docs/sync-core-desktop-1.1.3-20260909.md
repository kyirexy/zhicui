# 抖音收藏真实分页与分类恢复修复（Windows 1.1.3）

## 状态

前端回归及真实账号收藏、喜欢各 50 条全序核对已完成。受影响收藏台账校准、Windows 安装包及正式网页发布结果仍待下方最终记录；现场采集通过不等于已部署。

## 现场根因

- 收藏官方列表采用 POST 请求，请求游标位于请求体。1.1.2 只检查 URL 查询参数，缺少游标时默认当作 `0`，因此滚动后的后续页可能清空已采集前缀，再被登记为新的首页排名。此前针对 URL 中混合游标的修复没有覆盖这条真实请求链路。
- 原先通过“收藏”可见文本点击标签，没有严格等待官方标签出现和选中状态稳定。官网水合时实际会吞掉最初点击，页面仍停留在“作品”；该轮没有取得收藏接口首屏。本人主页也可能一直保留 `/user/self`，不能要求它必须跳转才能选择分类。
- 喜欢成功、收藏失败时，前端没有将喜欢结果覆盖到收藏。当前收藏页保留旧台账的来源排名；历史采集若曾把后页误登记为可信首页，单纯刷新网页无法校正该台账。
- 卡片日期优先来自视频发布时间，与收藏顺序不是同一个字段。旧年份的视频也可能是当前最近收藏，不能依据发布日期推断未同步或排序错误。

## 实现范围

### Windows 原生采集

- 采集请求 URL、method 和 POST body；解析 JSON 或表单中的来源游标。收藏使用 `cursor`，喜欢和作品使用各自游标规则；请求体与查询参数冲突、游标缺失或非法时，不再默认为首屏。
- 按官方游标连续连接列表，保留首屏到后续页的先后关系，不能用网络完成顺序或视频发布日期替代收藏顺序。
- 官网在滚动过程中可能自行重发首页。最多保留相邻两轮独立页链；新轮首页及所有已返回重叠页的完整身份序列、游标与结束标记相同，且上一轮独立满足所选数量时，可保留那一轮完整结果。新轮缺失/变化、身份缺口或未知游标时不回退，不跨轮拼接后页；新轮自行读满时优先使用新轮。
- 使用官方标签 ID 和 `role=tab` 定位喜欢、收藏和作品，分开计算页面加载与标签选择等待时间，确认 `aria-selected=true` 稳定后继续。仅在仍未选中时最多点击三次，每次完成后至少间隔两秒；已选中的标签不重复点击。
- 真实 DOM 的活动 pane 只有零尺寸动画占位，卡片位于兄弟区域。沿精确已选标签的 aria 关联活动面板寻找实际滚动祖先，不再要求占位面板内部存在视频链接。资料仅来自对应分类的官方列表响应。
- 只在资料数量和实际滚动位置均不再前进时累计停滞。长列表尚未滚到分页触发区时，不因五轮数量相同提前停止；保留 36 轮与挂起请求时限，初始化回到顶部只执行一次。

### 网页入口与展示

- `MIN_LOCAL_DOUYIN_DESKTOP_VERSION` 提升为 `1.1.3`，版本比较使用该常量作为唯一最低版本来源。
- 资料库与 Agent 的抖音本机同步入口均先检查版本。1.1.2 加载新网页后会提示升级，不开始本机采集，也不绕过该检查走旧收藏同步分支。B站、小红书、普通链接导入和既有文稿任务不受此抖音能力门槛影响。
- 切换抖音分类时同步更新 URL 中的 `mode`，保留其余查询参数、锚点与浏览器历史状态。由首页收藏入口切换到喜欢后，刷新不会再被旧 URL 拉回收藏。
- 卡片桌面视图标注“发布于”；缺少发布日期且显示记录日期时使用“记录于”。窄屏两列卡片保留短日期，并通过完整提示及无障碍标签说明日期含义。保持原日期来源与排序规则。
- 右侧资料预览同步补上日期含义：抖音的原发布日期标为“发布于”，记录日期回退标为“记录于”；跨平台原发布日期仍优先，导入日期回退标为“导入于”。只说明现有日期来源，不更改日期映射或来源排序。
- 抖音专用页及首页继续沿用服务端当前分类的可靠快照与排名；缓存仍按账号、分类、排序隔离。同步失败保留既有资料，成功同步不会用小批次替换全部历史。
- 收藏台账校准随网页发布一次性失效旧来源快照：抖音列表缓存从 v4 提升到 v5，包含来源预览的首页会话缓存从 v7 提升到 v8，避免网络响应前闪回旧错误排名。跨平台列表缓存仍为 v2；既有网络刷新及同账号部分失败回退机制保持不变。

## 前端验收

- 最终独立审查确认：抖音本机登记调用方为资料库与 Agent 同步窗口，均受 1.1.3 门槛约束；跨平台账号面板的实际入口仅为 B站和小红书；旧自动同步调度器未挂载，历史自动同步实现无调用入口。
- 17 个既有 Node 测试文件共 82 项通过，覆盖版本门槛、两个真实页面同步入口、来源独立排序、URL 恢复、缓存隔离与失效、迟到请求、部分成功、重复复用、文稿进度以及首页刷新。未新增测试框架。
- 右侧日期语义补充后新增 2 项日期来源回归，与分类 URL 和来源排序合计 11 项定向复验通过，确认仅增加标签、不替换发布日期或回退日期。
- 缓存版本更新后，列表缓存、真实首页刷新、同步通知及首页启动合计 23 项回归通过，覆盖拒读旧 v4/v7、写入新 v5/v8、跨平台 v2 继续可用及旧请求失效。
- `npx tsc --noEmit --pretty false` 完整检查通过（退出码 0）。
- 对照 `vercel:react-best-practices` 复核本轮 TSX 修改：导航更新放在用户事件中，不新增 effect 或订阅；沿用 Next.js 16 支持的原生 History API；不引入 React DOM 所有权变更或弹层。

复验命令（在 `frontend` 执行）：

```powershell
node --experimental-strip-types --test src/lib/douyinDesktopSync.test.ts src/lib/douyinSyncFeedback.test.ts src/lib/platformSyncFeedback.test.ts src/lib/platformLibraryOrder.test.ts src/lib/platformImportBatch.test.ts src/lib/libraryTranscriptPreparation.test.ts src/lib/librarySyncProgress.test.ts src/lib/libraryMultiSourceSync.test.ts src/lib/librarySourceNavigation.test.ts src/lib/libraryListCache.test.ts src/lib/libraryUpdates.test.ts src/lib/libraryIncrementalSync.test.ts src/lib/librarySyncDiff.test.ts src/lib/workspaceHomeRefresh.test.ts src/lib/homeSourceClassification.test.ts src/lib/homeBootstrap.test.ts src/lib/manualSyncPolicy.test.ts
npx tsc --noEmit --pretty false
```

## 待完成的现场及发布记录

- [x] 真实账号收藏、喜欢分别连续前 50 条：原生采集与独立重建的官方请求游标链逐条一致，`orderReliable=true`、`coverage=limited`。该结果表示各分类本次前 50 条，未宣称读取所有作品。喜欢于北京时间 18:38:37、收藏于 18:39:28 完成，均只点击一次即选中。
- [ ] 受影响收藏台账：完成备份、仅针对已确认错误的收藏排名校准，并回读核对；保留历史资料及已有文稿，不把校准当作新增提取任务。
- [x] 原生编译及回归：`npm run build`、`node scripts/verify-platform-account.mjs` 与 diff 检查通过。覆盖 POST 游标、未知/冲突游标、相邻两轮完整列表隔离、首页/重叠页变化、身份缺口、慢加载和水合点击、空动画面板、长列表第六轮继续、到底五轮停止。最终两类实采编译模块 SHA-256 均为 `c5abdc9d29798a02f1f5a702fc05305fd931b921185ec8aa5f4ca39192fbd99b`。
- [ ] Windows 1.1.3：补记隔离构建、安装包大小与哈希、签名/发行契约、Beta 通道和公网完整回读结果。
- [ ] 正式网页：补记提交、Jenkins 构建号、备份与恢复检查、运行目录、健康检查、构建标识和真实旅程结果。
- [ ] 本机安装：确认实际运行版本为 1.1.3；网页刷新本身不会更新客户端原生采集代码。

私人收藏作品 ID、完整收藏列表、平台会话及原始响应正文不写入仓库文档。现场取证与数据修复明细保留在受控本地记录中。

首次生产只读预演：50 条来源排名校准，新增 14 条公开元数据、16 个收藏成员，47 份既有文稿保持；收藏台账预计从 112 增至 128，未出现在本次前 50 条中的历史成员全部保留。删除和 ASR 调用均为 0。正式写入前再次核对最终采集证据与数据库 CAS，不将本预演当作已执行。
