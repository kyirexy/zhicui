# 同步读取与进度反馈修复（Windows 1.1.2）

## 问题与现场核对

- 反馈发生时，线上网页已是 `5e915976807814da4aa2fd33765f528d54559c86`（Jenkins #211），当前 Windows 客户端仍为 1.1.1。网页刷新不能替换客户端内置的平台读取代码。
- 只读核对本次同步记录：收藏新增 10 条、喜欢复用 20 条，登记均成功；对应文稿批次当时为 9 条完成、1 条转写中。同步窗口的“将在后台准备”未跟随真实任务状态更新，且覆盖了新增/复用摘要。
- 后续只读回查，该批次已成功完成全部 10 条文稿；没有因诊断重新同步或重建用户任务。
- 代码及回归复现了收藏 URL 同时带 `cursor` 与固定 `max_cursor=0` 时，后页误被识别为首屏的问题。没有将该复现当作此次真实平台网络响应的完整诊断。

## 修改

- 收藏按自身 `cursor`，喜欢/作品按 `max_cursor` 连接官方分页；同一轮按游标而非响应完成时间排序，旧轮迟到响应不能拼入新轮。
- 只保留官方列表连续可确认身份的前缀，不能跨过身份缺口后压缩排名。已确认所选前 N 条时，不因范围外的失效占位误报为未完成；短前缀仍明确记录部分读取。
- 同步窗口保留检查、新增、复用摘要并更新文稿实际进度；相同来源诊断合并显示，正常按所选范围读取不再泛化为错误。账号验证、平台限流和真实部分读取仍明确说明。
- 账号切换周期、文稿任务归属和同步批次共同约束迟到响应；即使账号 A→B→A，也不能由旧任务写回缓存、覆盖进度或清除新任务运行标记。
- 普通同步仅自动准备服务端明确标记为新增的作品，跨来源去重，已完整文稿直接复用；补齐旧文稿仍由明确操作触发。
- 保持已上线的增量保存、历史保留、来源独立排名、同步记录和缓存版本约束。
- 纳入上轮已验证的 6 个废弃组件、6 张无引用 SVG 清理及 4 条工具产物忽略规则；未将主工作区其他未完成改动混入发布。清理背景见 `project-cleanup-20260909.md`。

## 发布约束与记录

- Windows 沿用当前 Beta 更新通道，版本提升为 1.1.2。安装包由固定提交的干净隔离工作树生成，经来源、文件大小、SHA-256/SHA-512 和公网回读验证后发布。
- 正式网页由 Jenkins 执行加密备份、隔离恢复、生产构建、原子切换、健康与真实旅程验收。
- 服务器发布前在部署锁内仅清理已停用 Jenkins #207 的 `.venv`、`frontend/node_modules`、`frontend/.next` 可重建缓存；保留 #209、#210、当前 #211、源代码、备份及业务资料。可用空间由约 2.43 GB 增至 4.63 GB。
- 本机现有客户端安装在 `C:\Program Files\zhicui-desktop`，属于所有用户安装。升级需要沿用该作用域，Windows 可能要求 UAC；公网发布完成不等于本机已安装。

## 已完成验收

- 桌面平台读取回归与 TypeScript 编译通过，覆盖混合游标、跨轮迟到后页、范围外占位、身份缺口和未知终点。
- Windows 1.1.2 从提交 `c8f102c93651ceabe54a991bc5afaf93b4a805ed` 隔离构建，Agent 接入及发行契约检查通过；现有 Beta 通道已上传安装包、blockmap 和更新清单。
- 安装包大小 93,582,586 字节，SHA-256 `ba27a3a557ea05738c70fc267861fb1fea23238abfdb7cffb6ad5bd92f356336`。本地来源及构建记录位于外部发行缓存，无密钥写入项目。
- 公开页面/鉴权发行契约 12 项回归通过。
- 前端 45 项同步、文稿准备、真实页面异步函数与首页刷新定向测试通过；最后状态清理后其中 10 项复验通过。再补跑 15 项来源顺序和缓存回归，与公开发行契约合计 72 项不同用例通过。前端完整 TypeScript 检查通过，已覆盖本次废弃组件和资源清理；未新增测试框架。
- 对照 `vercel:react-best-practices` 检查了 effect 依赖、异步闭包、订阅清理和 transient refs；独立审查发现的同账号往返切换及旧接口新增 ID 回退问题已修复。

公网产物回读和网页生产发布结果在完成后追加。
