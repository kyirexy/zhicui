## 1. 内容问答后端

- [x] 1.1 在 `ai_juicer` 中实现有上下文预算、来源约束和历史限制的笔记问答函数
- [x] 1.2 新增用户范围的 `POST /api/notes/{note_id}/ask` 请求模型、校验和响应端点

## 2. 内容问答前端

- [x] 2.1 扩展前端 API 与类型，支持发送当前问题和最近会话历史
- [x] 2.2 实现响应式 `ContentChat`，包含推荐问题、消息、等待态、错误重试和清空会话
- [x] 2.3 在所有带 `noteId` 的卡片结果后挂载问答组件且不污染 PNG 导出区域

## 3. 卡片风格系统

- [x] 3.1 安装 `@phosphor-icons/react` 并实现可复用的动态图标和风格缩略预览组件
- [x] 3.2 新增 Aurora、Blueprint、Paper 三套响应式卡片组件并注册到 `CardStyle` 映射
- [x] 3.3 将桌面工具栏、移动底部抽屉、全局抽屉和风格页升级为九套可视化预览选择器
- [x] 3.4 增加新卡片、聊天、选择器的主题样式、交互反馈和 reduced-motion 降级

## 4. 验证与运行

- [x] 4.1 执行后端语法/导入检查与前端生产构建，修复所有新增问题
- [x] 4.2 启动前后端，检查健康端点与核心页面在桌面/移动视口的可用性
