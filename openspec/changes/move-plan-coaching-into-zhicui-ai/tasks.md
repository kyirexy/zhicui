## 1. Agent 计划上下文

- [x] 1.1 为 Agent Thread 增加兼容旧数据的领域上下文字段和可加性启动迁移
- [x] 1.2 扩展 Thread 创建与序列化 API，安全创建单计划会话并拒绝跨用户 Plan ID
- [x] 1.3 让持久 Turn 在无视频来源时仍能恢复计划上下文

## 2. 计划预览与确认工具

- [x] 2.1 实现使用当前知萃 AI 模型生成计划变更预览的专用 Agent 执行路径
- [x] 2.2 将结构化预览持久为 Agent Assistant Message，并接入 Turn 事件、额度捕获与失败释放
- [x] 2.3 新增只接收 Message ID 的计划变更确认 API，支持所有权验证、冲突检测和重复确认幂等

## 3. 前端产品收口

- [x] 3.1 移除计划页内嵌 AI Coach，改为可触控的“让知萃 AI 帮我调整”深链接入口
- [x] 3.2 让知萃 AI 识别 `plan_id`，创建/恢复 Plan Thread 并显示计划上下文而不是视频资料计数
- [x] 3.3 在对话消息中展示可恢复的计划 diff 卡，完成确认应用、冲突提示和查看计划闭环

## 4. 验证

- [x] 4.1 增加后端测试，覆盖所有权、预览不写入、确认应用、重复确认、冲突和计费失败释放
- [x] 4.2 运行前端类型检查与相关测试，并在 Web 端验证从计划进入、预览、刷新恢复和确认后回到计划的完整流程
